<%= form_for [@post, @comment], :remote => true do |f| %><%# form_for [@post, @comment] do |f| %>
    <p>
      <%= f.label :author %>
      <%= f.text_field :author %>
    </p>
    <p>
      <%= f.label :content %>
      <%= f.text_area :content %>
    </p>
    <%= f.submit "Post" %>
<% end %>
